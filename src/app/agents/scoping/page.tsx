import { ScopingAgentWorkspace } from "@/components/agents/ScopingAgentWorkspace";

export const metadata = {
  title: "Scoping Agent | PE Growth Diagnostic",
  description: "Agent 1 — Interview guides and fact-base for value sizing",
};

export default function ScopingAgentPage() {
  return <ScopingAgentWorkspace />;
}
